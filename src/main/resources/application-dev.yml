server.port: 8081
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/forum
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        show-sql: true
        format-sql: true
  flyway.locations: db
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379

jwt:
  secret: secreto
